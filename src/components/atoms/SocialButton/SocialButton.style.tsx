import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    base:{
        borderWidth: 2,
        padding: 8,
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
    }
})