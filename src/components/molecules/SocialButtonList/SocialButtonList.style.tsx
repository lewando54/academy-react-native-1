import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    div: {
        display: 'flex',
        width: '100%',
        justifyContent: 'center',
        gap: 16,
        flexDirection: 'row'
    }
})